@{
    Layout = "_AdminLayout";
    ViewData["Title"] = "SQL Injection Lab";
}

<h2>🛡️ SQL Injection Laboratory</h2>

<div class="alert alert-warning">

Bu laboratuvarda güvenli ve güvensiz SQL sorgularını
karşılaştıracağız.

</div>

<h4>Güvensiz Kod</h4>

<pre>
var sql =
"SELECT * FROM Events WHERE Title = '" + search + "'";
</pre>

<h4>Saldırı</h4>

<pre>
' OR 1=1 --
</pre>

<h4>Sonuç</h4>

<p>

Tüm kayıtlar döner.

</p>

<hr/>

<h4>Güvenli Kod (Entity Framework)</h4>

<pre>
_context.Events
.Where(e => e.Title.Contains(search))
</pre>

<p class="text-success">

✔ Entity Framework parametreli sorgu kullanır.

</p>